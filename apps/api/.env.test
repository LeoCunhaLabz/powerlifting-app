# Valores fictícios apenas para satisfazer a validação de env.ts durante `npm run test:api`.
# Os testes usam um `db` mockado — nenhuma conexão real com Postgres é aberta.
DATABASE_URL=postgresql://test:test@localhost:5432/test
JWT_SECRET=test-jwt-secret-with-at-least-32-characters
