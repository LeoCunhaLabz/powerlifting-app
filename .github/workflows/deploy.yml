name: Deploy

# Pipeline de produção: roda em todo push para `main`.
# Fluxo: build (gate) -> deploy via API do Dokploy -> smoke test -> e-mail.
# Como o deploy só é disparado APÓS o build passar, código quebrado nunca sobe.
# Pré-requisito: desligue o auto-deploy do Dokploy para evitar deploy duplicado.
on:
  push:
    branches: ["main"]

# Garante um deploy por vez (não cancela o que já está rodando).
concurrency:
  group: deploy-main
  cancel-in-progress: false

jobs:
  # 1) Gate: lint + build (type-check). Se falhar, o deploy não acontece.
  build:
    name: Lint & Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Instalar dependências
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Testes
        run: npm run test

      - name: Build (type-check + bundle)
        run: npm run build

  # 2) Deploy: dispara o build/deploy no Dokploy via webhook.
  deploy:
    name: Deploy no Dokploy
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Disparar deploy no Dokploy
        env:
          DOKPLOY_WEBHOOK_URL: ${{ secrets.DOKPLOY_WEBHOOK_URL }}
        run: |
          echo "Disparando deploy via webhook..."
          http_code=$(curl -sS -o response.json -w "%{http_code}" \
            -X POST "$DOKPLOY_WEBHOOK_URL")
          echo "HTTP $http_code"
          cat response.json || true
          if [ "$http_code" -lt 200 ] || [ "$http_code" -ge 300 ]; then
            echo "::error::Falha ao disparar o deploy no Dokploy (HTTP $http_code)"
            exit 1
          fi

  # 3) Smoke test: confirma que a nova versão está no ar (HTTP 200).
  verify:
    name: Smoke test
    needs: deploy
    runs-on: ubuntu-latest
    outputs:
      status: ${{ steps.healthcheck.outputs.status }}
    steps:
      - name: Aguardar e verificar a aplicação
        id: healthcheck
        env:
          APP_URL: ${{ secrets.APP_URL }}
        run: |
          echo "Verificando $APP_URL (o deploy do Dokploy é assíncrono)..."
          # Dá um tempo inicial para o Dokploy começar o build/rollout.
          sleep 30
          attempts=20
          for i in $(seq 1 $attempts); do
            code=$(curl -sS -o /dev/null -w "%{http_code}" --max-time 15 "$APP_URL" || echo "000")
            echo "Tentativa $i/$attempts -> HTTP $code"
            if [ "$code" = "200" ]; then
              echo "status=ok" >> "$GITHUB_OUTPUT"
              echo "Aplicação respondeu 200. Nova versão no ar."
              exit 0
            fi
            sleep 15
          done
          echo "status=fail" >> "$GITHUB_OUTPUT"
          echo "::error::A aplicação não respondeu 200 após o deploy."
          exit 1

  # 4) E-mail: resumo do pipeline. Roda sempre (sucesso ou falha).
  notify:
    name: E-mail de resumo
    needs: [build, deploy, verify]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Montar resumo
        id: summary
        run: |
          build="${{ needs.build.result }}"
          deploy="${{ needs.deploy.result }}"
          verify="${{ needs.verify.result }}"
          icon() { case "$1" in success) echo "✅";; failure) echo "❌";; skipped) echo "⏭️";; *) echo "⚠️";; esac; }
          if [ "$build" = "success" ] && [ "$deploy" = "success" ] && [ "$verify" = "success" ]; then
            overall="✅ SUCESSO — nova versão no ar"
          else
            overall="❌ FALHA — verifique o pipeline"
          fi
          {
            echo "subject=[Powerlifting App] Deploy ${overall}"
            echo "overall=${overall}"
            echo "build_icon=$(icon "$build")"
            echo "deploy_icon=$(icon "$deploy")"
            echo "verify_icon=$(icon "$verify")"
          } >> "$GITHUB_OUTPUT"

      - name: Enviar e-mail (Resend)
        env:
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          MAIL_TO: ${{ secrets.MAIL_TO }}
        run: |
          # Grava o payload JSON em arquivo para evitar problemas de escaping no shell.
          cat > /tmp/email.json <<'ENDJSON'
          {
            "from": "Powerlifting CI <onboarding@resend.dev>",
            "to": ["MAIL_TO_PLACEHOLDER"],
            "subject": "SUBJECT_PLACEHOLDER",
            "html": "HTML_PLACEHOLDER"
          }
          ENDJSON

          # Substitui os placeholders com os valores reais.
          SUBJECT="${{ steps.summary.outputs.subject }}"
          HTML="<h2>${{ steps.summary.outputs.overall }}</h2><p><b>Repositório:</b> ${{ github.repository }}<br/><b>Branch:</b> ${{ github.ref_name }}<br/><b>Commit:</b> <code>${{ github.sha }}</code><br/><b>Autor:</b> ${{ github.actor }}</p><h3>Etapas</h3><ul><li>${{ steps.summary.outputs.build_icon }} Lint, Testes &amp; Build</li><li>${{ steps.summary.outputs.deploy_icon }} Deploy no Dokploy</li><li>${{ steps.summary.outputs.verify_icon }} Smoke test (app no ar)</li></ul><p><a href=\"${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\">Ver detalhes no GitHub Actions</a></p>"

          python3 -c "
import json, os
with open('/tmp/email.json') as f:
    data = json.load(f)
data['to'] = [os.environ['MAIL_TO']]
data['subject'] = os.environ['SUBJECT']
data['html'] = os.environ['HTML']
with open('/tmp/email.json', 'w') as f:
    json.dump(data, f)
" MAIL_TO="$MAIL_TO" SUBJECT="$SUBJECT" HTML="$HTML"

          http_code=$(curl -sS -o /tmp/resend_response.json -w "%{http_code}" \
            -X POST 'https://api.resend.com/emails' \
            -H "Authorization: Bearer $RESEND_API_KEY" \
            -H 'Content-Type: application/json' \
            -d @/tmp/email.json)
          echo "Resend HTTP $http_code"
          cat /tmp/resend_response.json || true
          if [ "$http_code" -lt 200 ] || [ "$http_code" -ge 300 ]; then
            echo "::warning::Falha ao enviar e-mail via Resend (HTTP $http_code), mas o deploy foi realizado."
          fi
